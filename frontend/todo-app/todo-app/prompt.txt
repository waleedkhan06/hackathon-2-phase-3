SYSTEM UPGRADE PROMPT — TODO AI CHAT ASSISTANT

You are acting as a senior full-stack engineer + AI agent architect.

The backend and frontend are already working perfectly.
Do NOT break existing Phase II functionality.
Your task is to improve and extend the AI chatbot system with the requirements below.

1️⃣ Backend Improvements (Critical)
A. Message & Task Schema Fix

Currently, chatbot messages store only content

Tasks elsewhere have title + description

This causes inconsistency and poor AI understanding

Required changes:

Update backend Message model to include:

title (optional)

description (optional)

Update database schema and migrations safely

Ensure backward compatibility with existing records

B. AI Agent Intelligence Upgrade (Very Important)

The AI assistant is currently too rigid and not conversational.

You MUST:

Improve the AI agent config

Use a parser-based approach (already in use)

Handle open-ended user input, not just commands

The AI should:

Respond naturally to greetings (e.g. “hello”, “hi”, “help”)

Guide the user step-by-step when intent is unclear

Ask follow-up questions when required data is missing

Never fail silently

Examples:

User: “add task”

AI: “Sure 👍 What’s the task title and description?”

User: “update task”

AI: “Please tell me the task ID or number you want to update.”

C. Task Identification in Chat

Chat responses MUST include:

Task ID

Task number (index)

This allows users to reference tasks easily in follow-ups

Example AI response:

“Task #3 (ID: a8f3…) has been updated successfully.”

D. Fix Delayed Task Visibility Bug

Current bug:

When tasks are added via AI:

They only appear after page refresh

Fix required:

Ensure task creation via AI:

Updates DB immediately

Returns updated task list or event

Frontend reflects changes without refresh

No polling hacks.
Use proper state sync.

2️⃣ Frontend Improvements
A. Dedicated AI Chat Page (NOT Sidebar)

Current chatbot placement is not acceptable.

Required:

Create a separate AI Assistant page

Add a navigation link in the header

The chat assistant must:

Have its own route

Be isolated from the task list UI

Preserve conversation state

B. Chat UI Enhancements

Display:

Task ID

Task number

Clear assistant vs user messages

Friendly tone and confirmations

3️⃣ Auth UX Fixes (Very Important)
A. Sign-Up Error Handling

Current issue:

“Failed to fetch” error shown to users

Fix:

If user already exists:

Show friendly message:

“An account with this email already exists. Please sign in.”

Never expose raw backend errors to users

B. Sign-In Error Handling

No technical errors

Show simple, human-readable messages:

Invalid email/password

User not found

4️⃣ Constraints (DO NOT VIOLATE)

❌ Do NOT break Phase II APIs

❌ Do NOT store AI state in memory

✅ DB must remain the single source of truth

✅ Backend must remain stateless

✅ All changes must be production-safe

5️⃣ Final Deliverables

You must:

Update backend models + migrations

Improve AI agent configuration and parsing logic

Fix chat task sync bug

Create a dedicated AI chat page in frontend

Improve auth UX error handling

Ensure existing functionality remains untouchedi